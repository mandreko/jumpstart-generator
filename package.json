{
  "name": "jumpstart-generator",
  "version": "2.0.0",
  "description": "MTG Jumpstart Card Conjurer JSON Generator",
  "scripts": {
    "clean": "rimraf \"output\"",
    "convert": "node run-convert-all.js",
    "generate": "node run-generate-all.js",
    "render": "node run-render-all.js",
    "build": "npm run convert && npm run generate",
    "rebuild": "npm run clean && npm run build && npm run render"
  },
  "keywords": [
    "mtg",
    "magic-the-gathering",
    "jumpstart",
    "card-conjurer"
  ],
  "author": "",
  "license": "ISC",
  "engines": {
    "node": ">=14.0.0"
  },
  "devDependencies": {
    "playwright": "^1.40.0",
    "rimraf": "^6.0.0"
  }
}
